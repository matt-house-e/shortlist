"""REST API module (Optional placeholder).

This module is reserved for REST API endpoints if needed.
The main chat interface uses Chainlit's built-in WebSocket handling.

Example usage for adding API endpoints:

    from fastapi import FastAPI, APIRouter
    from app.api import router

    # In your FastAPI app setup:
    app = FastAPI()
    app.include_router(router, prefix="/api")
"""

# Placeholder for future API implementation
# from fastapi import APIRouter
# router = APIRouter()
